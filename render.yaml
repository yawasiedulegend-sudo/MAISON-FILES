services:
  - type: web
    name: maison-boutique-api
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python backend.py
    healthCheckPath: /api/orders
